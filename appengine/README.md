To use this package, perform the following steps:
  1. `cp app.yaml.sample app.yaml`.
  2. Modify app.yaml, inserting your application name for GAE.
  3. Copy your lmk.conf and rules.conf into this directory.
  4. Run `appcfg.py --oauth2 update .`.
